import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:{{ProjectNameSnake}}/features/home/home_screen.dart';
import 'package:{{ProjectNameSnake}}/l10n/app_localizations.dart';

import '../../golden_harness.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  for (final locale in AppLocalizations.supportedLocales) {
    testWidgets('home screen golden (${locale.languageCode})', (tester) async {
      await GoldenHarness.pumpGolden(
        tester,
        HomeScreen(previewStatus: locale.languageCode == 'de' ? 'Gesund' : 'Healthy'),
        locale,
        'goldens/home_screen_${locale.languageCode}.png',
      );
    });
  }
}
