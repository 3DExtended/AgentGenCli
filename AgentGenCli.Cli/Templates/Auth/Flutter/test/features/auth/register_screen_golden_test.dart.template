import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:{{ProjectNameSnake}}/features/auth/register_screen.dart';
import 'package:{{ProjectNameSnake}}/l10n/app_localizations.dart';

import '../../golden_harness.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  for (final locale in AppLocalizations.supportedLocales) {
    testWidgets('register screen golden (${locale.languageCode})', (tester) async {
      await GoldenHarness.pumpGolden(
        tester,
        const RegisterScreen(),
        locale,
        'goldens/register_screen_${locale.languageCode}.png',
      );
    });
  }
}
